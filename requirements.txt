# Keep in sync with [project.dependencies] in pyproject.toml.
# Exists separately so the Dockerfile can cache the dependency layer
# without copying the whole source tree.
clize>=5.0.0
docxtpl>=0.16.0
python-docx>=1.0.0
lxml>=5.0.0
jinja2>=3.1.0
markdown-it-py>=3.0.0
mdit-py-plugins>=0.4.0
pyyaml>=6.0
